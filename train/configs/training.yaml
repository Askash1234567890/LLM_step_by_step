training:
  num_epochs: 1
  learning_rate: 2.5e-4
  weight_decay: 0.1
  grad_clip: 1.0
  grad_accumulation_steps: 1
  use_amp: false          # true only for cuda
  warmup_steps: 100
  train_batch_size: 8
  valid_batch_size: 8
  num_workers: 8
  device: mps

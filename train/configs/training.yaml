training:
  num_epochs: 10
  learning_rate: 3.0e-4
  weight_decay: 0.1
  grad_clip: 1.0
  grad_accumulation_steps: 1
  use_amp: false          # true only for cuda
  warmup_steps: 100
  device: cpu
  train_batch_size: 32
  valid_batch_size: 32
  num_workers: 4
